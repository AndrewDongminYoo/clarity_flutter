// Autogenerated from Pigeon (v26.1.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.microsoft.clarity.generated

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer

private object MessagesPigeonUtils {
    fun wrapResult(result: Any?): List<Any?> = listOf(result)

    fun wrapError(exception: Throwable): List<Any?> =
        if (exception is FlutterError) {
            listOf(
                exception.code,
                exception.message,
                exception.details,
            )
        } else {
            listOf(
                exception.javaClass.simpleName,
                exception.toString(),
                "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception),
            )
        }

    fun deepEquals(
        a: Any?,
        b: Any?,
    ): Boolean {
        if (a is ByteArray && b is ByteArray) {
            return a.contentEquals(b)
        }
        if (a is IntArray && b is IntArray) {
            return a.contentEquals(b)
        }
        if (a is LongArray && b is LongArray) {
            return a.contentEquals(b)
        }
        if (a is DoubleArray && b is DoubleArray) {
            return a.contentEquals(b)
        }
        if (a is Array<*> && b is Array<*>) {
            return a.size == b.size &&
                a.indices.all { deepEquals(a[it], b[it]) }
        }
        if (a is List<*> && b is List<*>) {
            return a.size == b.size &&
                a.indices.all { deepEquals(a[it], b[it]) }
        }
        if (a is Map<*, *> && b is Map<*, *>) {
            return a.size == b.size &&
                a.all {
                    (b as Map<Any?, Any?>).contains(it.key) &&
                        deepEquals(it.value, b[it.key])
                }
        }
        return a == b
    }
}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError(
    val code: String,
    override val message: String? = null,
    val details: Any? = null,
) : Throwable()

enum class ConnectivityType(
    val raw: Int,
) {
    WIFI(0),
    MOBILE(1),
    ETHERNET(2),
    OTHER(3),
    NONE(4),
    ;

    companion object {
        fun ofRaw(raw: Int): ConnectivityType? = values().firstOrNull { it.raw == raw }
    }
}

/** Generated class from Pigeon that represents data sent in messages. */
data class DeviceInfoData(
    val brand: String? = null,
    val machine: String? = null,
    val totalMemory: Long,
) {
    companion object {
        fun fromList(pigeonVar_list: List<Any?>): DeviceInfoData {
            val brand = pigeonVar_list[0] as String?
            val machine = pigeonVar_list[1] as String?
            val totalMemory = pigeonVar_list[2] as Long
            return DeviceInfoData(brand, machine, totalMemory)
        }
    }

    fun toList(): List<Any?> =
        listOf(
            brand,
            machine,
            totalMemory,
        )

    override fun equals(other: Any?): Boolean {
        if (other !is DeviceInfoData) {
            return false
        }
        if (this === other) {
            return true
        }
        return MessagesPigeonUtils.deepEquals(toList(), other.toList())
    }

    override fun hashCode(): Int = toList().hashCode()
}

/** Generated class from Pigeon that represents data sent in messages. */
data class PackageInfoData(
    val packageName: String,
    val version: String,
    val buildNumber: String,
) {
    companion object {
        fun fromList(pigeonVar_list: List<Any?>): PackageInfoData {
            val packageName = pigeonVar_list[0] as String
            val version = pigeonVar_list[1] as String
            val buildNumber = pigeonVar_list[2] as String
            return PackageInfoData(packageName, version, buildNumber)
        }
    }

    fun toList(): List<Any?> =
        listOf(
            packageName,
            version,
            buildNumber,
        )

    override fun equals(other: Any?): Boolean {
        if (other !is PackageInfoData) {
            return false
        }
        if (this === other) {
            return true
        }
        return MessagesPigeonUtils.deepEquals(toList(), other.toList())
    }

    override fun hashCode(): Int = toList().hashCode()
}

/**
 * Wraps the connectivity list because Pigeon's event-channel Dart generator
 * strips generic type arguments and would emit `Stream<List>` instead of
 * `Stream<List<ConnectivityType>>`.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class ConnectivityChangedEventData(
    val types: List<ConnectivityType>,
) {
    companion object {
        fun fromList(pigeonVar_list: List<Any?>): ConnectivityChangedEventData {
            val types = pigeonVar_list[0] as List<ConnectivityType>
            return ConnectivityChangedEventData(types)
        }
    }

    fun toList(): List<Any?> =
        listOf(
            types,
        )

    override fun equals(other: Any?): Boolean {
        if (other !is ConnectivityChangedEventData) {
            return false
        }
        if (this === other) {
            return true
        }
        return MessagesPigeonUtils.deepEquals(toList(), other.toList())
    }

    override fun hashCode(): Int = toList().hashCode()
}

private open class MessagesPigeonCodec : StandardMessageCodec() {
    override fun readValueOfType(
        type: Byte,
        buffer: ByteBuffer,
    ): Any? {
        return when (type) {
            129.toByte() -> {
                return (readValue(buffer) as Long?)?.let {
                    ConnectivityType.ofRaw(it.toInt())
                }
            }

            130.toByte() -> {
                return (readValue(buffer) as? List<Any?>)?.let {
                    DeviceInfoData.fromList(it)
                }
            }

            131.toByte() -> {
                return (readValue(buffer) as? List<Any?>)?.let {
                    PackageInfoData.fromList(it)
                }
            }

            132.toByte() -> {
                return (readValue(buffer) as? List<Any?>)?.let {
                    ConnectivityChangedEventData.fromList(it)
                }
            }

            else -> {
                super.readValueOfType(type, buffer)
            }
        }
    }

    override fun writeValue(
        stream: ByteArrayOutputStream,
        value: Any?,
    ) {
        when (value) {
            is ConnectivityType -> {
                stream.write(129)
                writeValue(stream, value.raw.toLong())
            }

            is DeviceInfoData -> {
                stream.write(130)
                writeValue(stream, value.toList())
            }

            is PackageInfoData -> {
                stream.write(131)
                writeValue(stream, value.toList())
            }

            is ConnectivityChangedEventData -> {
                stream.write(132)
                writeValue(stream, value.toList())
            }

            else -> {
                super.writeValue(stream, value)
            }
        }
    }
}

val MessagesPigeonMethodCodec = StandardMethodCodec(MessagesPigeonCodec())

/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface ClarityDeviceHostApi {
    fun getDeviceInfo(): DeviceInfoData

    fun getPackageInfo(): PackageInfoData

    fun getCacheDirectory(): String?

    fun getUserAgent(): String

    companion object {
        /** The codec used by ClarityDeviceHostApi. */
        val codec: MessageCodec<Any?> by lazy {
            MessagesPigeonCodec()
        }

        /** Sets up an instance of `ClarityDeviceHostApi` to handle messages through the `binaryMessenger`. */
        @JvmOverloads
        fun setUp(
            binaryMessenger: BinaryMessenger,
            api: ClarityDeviceHostApi?,
            messageChannelSuffix: String = "",
        ) {
            val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
            val taskQueue = binaryMessenger.makeBackgroundTaskQueue()
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityDeviceHostApi.getDeviceInfo$separatedMessageChannelSuffix",
                        codec,
                        taskQueue,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getDeviceInfo())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityDeviceHostApi.getPackageInfo$separatedMessageChannelSuffix",
                        codec,
                        taskQueue,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getPackageInfo())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityDeviceHostApi.getCacheDirectory$separatedMessageChannelSuffix",
                        codec,
                        taskQueue,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getCacheDirectory())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityDeviceHostApi.getUserAgent$separatedMessageChannelSuffix",
                        codec,
                        taskQueue,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getUserAgent())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
        }
    }
}

/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface ClarityNetworkHostApi {
    fun getConnectivityStatus(): List<ConnectivityType>

    companion object {
        /** The codec used by ClarityNetworkHostApi. */
        val codec: MessageCodec<Any?> by lazy {
            MessagesPigeonCodec()
        }

        /** Sets up an instance of `ClarityNetworkHostApi` to handle messages through the `binaryMessenger`. */
        @JvmOverloads
        fun setUp(
            binaryMessenger: BinaryMessenger,
            api: ClarityNetworkHostApi?,
            messageChannelSuffix: String = "",
        ) {
            val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityNetworkHostApi.getConnectivityStatus$separatedMessageChannelSuffix",
                        codec,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getConnectivityStatus())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
        }
    }
}

private class MessagesPigeonStreamHandler<T>(
    val wrapper: MessagesPigeonEventChannelWrapper<T>,
) : EventChannel.StreamHandler {
    var pigeonSink: PigeonEventSink<T>? = null

    override fun onListen(
        p0: Any?,
        sink: EventChannel.EventSink,
    ) {
        pigeonSink = PigeonEventSink<T>(sink)
        wrapper.onListen(p0, pigeonSink!!)
    }

    override fun onCancel(p0: Any?) {
        pigeonSink = null
        wrapper.onCancel(p0)
    }
}

interface MessagesPigeonEventChannelWrapper<T> {
    open fun onListen(
        p0: Any?,
        sink: PigeonEventSink<T>,
    ) {}

    open fun onCancel(p0: Any?) {}
}

class PigeonEventSink<T>(
    private val sink: EventChannel.EventSink,
) {
    fun success(value: T) {
        sink.success(value)
    }

    fun error(
        errorCode: String,
        errorMessage: String?,
        errorDetails: Any?,
    ) {
        sink.error(errorCode, errorMessage, errorDetails)
    }

    fun endOfStream() {
        sink.endOfStream()
    }
}

abstract class ConnectivityChangedStreamHandler : MessagesPigeonEventChannelWrapper<ConnectivityChangedEventData> {
    companion object {
        fun register(
            messenger: BinaryMessenger,
            streamHandler: ConnectivityChangedStreamHandler,
            instanceName: String = "",
        ) {
            var channelName: String = "dev.flutter.pigeon.clarity_flutter.ClarityNetworkEvents.connectivityChanged"
            if (instanceName.isNotEmpty()) {
                channelName += ".$instanceName"
            }
            val internalStreamHandler = MessagesPigeonStreamHandler<ConnectivityChangedEventData>(streamHandler)
            EventChannel(messenger, channelName, MessagesPigeonMethodCodec).setStreamHandler(internalStreamHandler)
        }
    }
}

/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface ClarityGaidHostApi {
    fun getGaid(): String?

    companion object {
        /** The codec used by ClarityGaidHostApi. */
        val codec: MessageCodec<Any?> by lazy {
            MessagesPigeonCodec()
        }

        /** Sets up an instance of `ClarityGaidHostApi` to handle messages through the `binaryMessenger`. */
        @JvmOverloads
        fun setUp(
            binaryMessenger: BinaryMessenger,
            api: ClarityGaidHostApi?,
            messageChannelSuffix: String = "",
        ) {
            val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
            val taskQueue = binaryMessenger.makeBackgroundTaskQueue()
            run {
                val channel =
                    BasicMessageChannel<Any?>(
                        binaryMessenger,
                        "dev.flutter.pigeon.clarity_flutter.ClarityGaidHostApi.getGaid$separatedMessageChannelSuffix",
                        codec,
                        taskQueue,
                    )
                if (api != null) {
                    channel.setMessageHandler { _, reply ->
                        val wrapped: List<Any?> =
                            try {
                                listOf(api.getGaid())
                            } catch (exception: Throwable) {
                                MessagesPigeonUtils.wrapError(exception)
                            }
                        reply.reply(wrapped)
                    }
                } else {
                    channel.setMessageHandler(null)
                }
            }
        }
    }
}
